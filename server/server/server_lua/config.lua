-- config = {}
--
-- -- 程序可以用 config.lua 来配置
-- -- 定义一个常量
-- config.host = 127.0.0.1
-- config.port = 3000

-- config = function ()
-- 	-- print("logging", a)
-- 	local t = { port = 3000, host = "127.0.0.1"}
-- 	return t
-- end

config = {
	port = 3000,
	host = "127.0.0.1"
}

-- return config
